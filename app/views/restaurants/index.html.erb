<h1>Tous vos restaurants</h1>

<% @restaurants.each do |restaurant| %>
  <%# <%= render restaurant %>
  <div>
      <%= render restaurant %>
      <%= link_to "voir infos", restaurant_path(restaurant) %>
  </div>
<% end %>
<%= link_to "ajouter un nouveau restaurant", new_restaurant_path %>
